---
interface Props {
  classList: string[];
}
const { classList } = Astro.props;

import { twMerge } from "tailwind-merge";
---

<div
  class={twMerge([
    "bg-background",
    "border-border-muted",
    "border-4",
    "shadow-card",
    "rounded-md",
    ...classList,
  ])}
>
  <slot />
</div>
